---
description: Print the Jack & Jill palette as this terminal paints it, with its contrast measured
allowed-tools: Bash(~/.claude/themes/terminal-app/preview.sh), Bash(~/.claude/themes/terminal-app/jj_statusline.py:*)
---

!`~/.claude/themes/terminal-app/preview.sh`

!`/usr/bin/python3 ~/.claude/themes/terminal-app/jj_statusline.py --audit`

Above is the palette as this terminal actually paints it, then the measured
contrast of every colour the status line uses against both grounds.

Say in one or two lines whether anything is below its floor, and nothing else.
Do not restate the palette, do not summarise the theme, do not suggest changes
unless a measurement failed.
